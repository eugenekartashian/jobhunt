interface:
  display_name: "Remember"
  short_description: "Save or restore session context"
  default_prompt: "Use $remember to save or restore the project session context."
policy:
  allow_implicit_invocation: true
